import {
  numberWithDigits
} from '../../../helpers/number-with-digits';
import { module, test } from 'qunit';

module('NumberWithDigitsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberWithDigits(42);
  assert.ok(result);
});
